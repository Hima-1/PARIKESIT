// Re-export of the canonical spacing tokens.
//
// The original implementation lives in `app_spacing.dart` for backward
// compatibility with existing imports. New code should import this file
// (`tokens/spacing.dart`) so the token folder is the single discoverable
// entry point for design tokens.
export '../app_spacing.dart';
